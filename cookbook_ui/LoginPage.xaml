<Page
    x:Class="cookbook_ui.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:cookbook_ui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <Ellipse Width="220" Height="220" Fill="Black" Margin="0,20"/>
            <TextBlock Text="Login:" Margin="25,0,0,0" />
            <TextBox x:Name="loginBox" Margin="20,5" PlaceholderText="Login" Background="#EEEEEE" />
            <TextBlock Text="Hasło:" Margin="25,15,0,0" />
            <PasswordBox x:Name="passBox" Margin="20,5" PlaceholderText="Hasło" Background="#EEEEEE" />
            <Button x:Name="SubmitBtn" Click="SubmitBtn_Click" Content="Zaloguj" Margin="20,20,20,0" 
                    HorizontalAlignment="Stretch" Height="40" />
        </StackPanel>
    </Grid>
</Page>
